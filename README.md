# barbell_backend

# remember to initialize venv each instance

# ensure mysql installed locally, PATH set in Environment settings, and Django installed

# install pillow as well for django dependencies

# TODO 
# correctly set base html (index) block and endblocks so that site content is correctly displayed